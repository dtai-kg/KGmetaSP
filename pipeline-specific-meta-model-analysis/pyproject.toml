[project]
name = "rdf2vec-meta-model"
version = "0.1.0"
description = "Meta-model baseline experiments for pipeline-specific configurations"
authors = [
    {name = "Your Name",email = "you@example.com"}
]
readme = "README.md"
requires-python = "3.11.11"
dependencies = [
    "pandas (==2.2.3)",
    "openml (==0.15.1)"
]
package-mode = false


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
